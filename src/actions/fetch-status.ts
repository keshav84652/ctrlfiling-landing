"use server";

// import { getStatus } from "@openstatus/react";

export async function fetchStatus() {
  // Placeholder status - in production this would fetch from OpenStatus
  return "operational";
}
